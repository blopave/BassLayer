import React from "react";
import ReactDOM from "react-dom/client";
import BassLayer from "./BassLayer";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BassLayer />
  </React.StrictMode>
);
